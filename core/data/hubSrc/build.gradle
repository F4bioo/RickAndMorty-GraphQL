plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}
apply from: "$rootDir/plugins/android-build.gradle"

android {
    namespace "${Config.applicationId}.hubSrc"
}

dependencies {
    // Libs

    // Modules
    implementation project(Modules.remote)
}
