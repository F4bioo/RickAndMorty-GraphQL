plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}
apply from: "$rootDir/plugins/android-build.gradle"

android {
    namespace "${Config.applicationId}.core.data.local"
    defaultConfig {
        buildConfigField "String", "PREFS_NAME", '"Prefs"'
    }
}
